/* CS Server Manager — Light Theme */

/* === Base === */
QWidget {
    font-size: 13px;
}

QMainWindow,
QDialog {
    background-color: palette(window);
}

/* === Buttons === */
QPushButton {
    background-color: palette(button);
    color: palette(button-text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    padding: 6px 14px;
}

QPushButton:hover {
    background-color: #f5f2e8;
    border-color: #8a7000;
}

QPushButton:pressed {
    background-color: #8a7000;
    color: #ffffff;
    border-color: #8a7000;
}

QPushButton:disabled {
    color: palette(placeholder-text);
    border-color: palette(dark);
}

QPushButton[accent="true"] {
    background-color: #8a7000;
    color: #ffffff;
    border: none;
    font-weight: bold;
}

QPushButton[accent="true"]:hover {
    background-color: #c8a800;
}

QPushButton[accent="true"]:pressed {
    background-color: #6c5800;
}

/* === Text Inputs === */
QLineEdit,
QTextEdit,
QPlainTextEdit {
    background-color: palette(base);
    color: palette(text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    padding: 4px 8px;
    selection-background-color: palette(highlight);
    selection-color: palette(highlighted-text);
}

QLineEdit:focus,
QTextEdit:focus,
QPlainTextEdit:focus {
    border-color: #8a7000;
}

QLineEdit:disabled,
QTextEdit:disabled,
QPlainTextEdit:disabled {
    color: palette(placeholder-text);
    background-color: #f5f8fc;
}

/* === Combo Boxes === */
QComboBox {
    background-color: palette(button);
    color: palette(button-text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    padding: 4px 8px;
}

QComboBox:hover {
    border-color: #8a7000;
}

QComboBox:focus {
    border-color: #8a7000;
}

QComboBox::drop-down {
    border: none;
    width: 22px;
}

QComboBox::down-arrow {
    image: url(:/assets/chevron-down-light.svg);
    width: 12px;
    height: 12px;
}

QComboBox QAbstractItemView {
    background-color: palette(base);
    color: palette(text);
    border: 1px solid palette(mid);
    selection-background-color: palette(highlight);
    selection-color: palette(highlighted-text);
    outline: 0;
}

/* === Check Boxes === */
QCheckBox {
    spacing: 8px;
    color: palette(window-text);
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border: 1px solid palette(mid);
    border-radius: 3px;
    background: palette(base);
}

QCheckBox::indicator:hover {
    border-color: #8a7000;
}

QCheckBox::indicator:checked {
    background-color: transparent;
    border-color: #c8a800;
    image: url(:/assets/check.svg);
}

/* === Group Boxes === */
QGroupBox {
    border: 1px solid palette(mid);
    border-radius: 6px;
    margin-top: 12px;
    padding-top: 8px;
    color: palette(window-text);
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 6px;
    color: #8a7000;
    font-weight: bold;
}

/* === Tab Widgets === */
QTabWidget::pane {
    border: 1px solid palette(mid);
    border-radius: 4px;
    top: -1px;
}

QTabBar::tab {
    background: palette(button);
    color: palette(button-text);
    border: 1px solid palette(mid);
    border-bottom: none;
    border-top-left-radius: 4px;
    border-top-right-radius: 4px;
    padding: 6px 16px;
    margin-right: 2px;
}

QTabBar::tab:selected {
    background: palette(base);
    color: #8a7000;
}

QTabBar::tab:hover:!selected {
    background: #f5f2e8;
    border-color: #8a7000;
}

/* === Scrollbars === */
QScrollBar:vertical {
    background: transparent;
    width: 8px;
    margin: 0;
}

QScrollBar::handle:vertical {
    background: palette(mid);
    border-radius: 4px;
    min-height: 24px;
}

QScrollBar::handle:vertical:hover {
    background: #8a7000;
}

QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0;
}

QScrollBar::add-page:vertical,
QScrollBar::sub-page:vertical {
    background: transparent;
}

QScrollBar:horizontal {
    background: transparent;
    height: 8px;
    margin: 0;
}

QScrollBar::handle:horizontal {
    background: palette(mid);
    border-radius: 4px;
    min-width: 24px;
}

QScrollBar::handle:horizontal:hover {
    background: #8a7000;
}

QScrollBar::add-line:horizontal,
QScrollBar::sub-line:horizontal {
    width: 0;
}

QScrollBar::add-page:horizontal,
QScrollBar::sub-page:horizontal {
    background: transparent;
}

/* === List / Table Widgets === */
QListWidget,
QTreeWidget,
QTableWidget {
    background-color: palette(base);
    color: palette(text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    outline: 0;
}

QListWidget::item:hover,
QTreeWidget::item:hover,
QTableWidget::item:hover {
    background-color: #f5f2e8;
}

QListWidget::item:selected,
QTreeWidget::item:selected,
QTableWidget::item:selected {
    background-color: palette(highlight);
    color: palette(highlighted-text);
}

QHeaderView::section {
    background-color: palette(button);
    color: palette(button-text);
    border: none;
    border-right: 1px solid palette(mid);
    border-bottom: 1px solid palette(mid);
    padding: 4px 8px;
    font-weight: bold;
}

/* === Tool Tips === */
QToolTip {
    background-color: palette(tool-tip-base);
    color: palette(tool-tip-text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    padding: 4px 8px;
}

/* === Frames === */
QFrame[frameShape="4"],
QFrame[frameShape="5"] {
    color: palette(mid);
}

/* === Status Bar === */
QStatusBar {
    background: palette(window);
    color: palette(window-text);
    border-top: 1px solid palette(mid);
}

/* === Spinbox === */
QSpinBox,
QDoubleSpinBox {
    background-color: palette(base);
    color: palette(text);
    border: 1px solid palette(mid);
    border-radius: 4px;
    padding: 4px 8px;
}

QSpinBox:focus,
QDoubleSpinBox:focus {
    border-color: #8a7000;
}

/* === Progress Bar === */
QProgressBar {
    background-color: palette(base);
    border: 1px solid palette(mid);
    border-radius: 4px;
    text-align: center;
    color: palette(text);
}

QProgressBar::chunk {
    background-color: #8a7000;
    border-radius: 3px;
}

/* === Sliders === */
QSlider::groove:horizontal {
    height: 4px;
    background: palette(mid);
    border-radius: 2px;
}

QSlider::handle:horizontal {
    background: #8a7000;
    border: none;
    width: 14px;
    height: 14px;
    margin: -5px 0;
    border-radius: 7px;
}

QSlider::sub-page:horizontal {
    background: #8a7000;
    border-radius: 2px;
}

/* === Toast notification === */
#toastNotification {
    background-color: #ffffff;
    border: 1px solid #8a7000;
    border-radius: 8px;
    color: #1a1d27;
}

/* === Number Spinner === */
#numberSpinner {
    border: 1px solid palette(mid);
    border-radius: 4px;
    background: palette(base);
}

#numberSpinnerDisplay {
    border: none;
    background: transparent;
    color: palette(text);
}

#numberSpinnerButtons {
    background: transparent;
    border-left: 1px solid palette(mid);
}

#numberSpinnerBtnUp,
#numberSpinnerBtnDown {
    background: transparent;
    border: none;
}

#numberSpinnerBtnUp:hover,
#numberSpinnerBtnDown:hover {
    background: #f5f2e8;
}

/* === Picker Drawer === */
#pickerDrawer {
    background-color: palette(base);
    border-right: 1px solid palette(mid);
}

/* === Header Bar === */
#headerBar {
    background-color: palette(base);
    border-bottom: 1px solid palette(mid);
}

/* === Game Selector === */
#gameSelector {
    font-size: 14px;
    font-weight: bold;
    padding: 4px 10px;
}

/* === Start / Stop Button === */
#startStopBtn {
    font-size: 13px;
    font-weight: bold;
    min-width: 130px;
    border-radius: 4px;
    padding: 8px 20px;
}

#startStopBtn[serverRunning="false"] {
    background-color: #e0f0e0;
    color: #2a6a2a;
    border: 1px solid #4a8a4a;
}

#startStopBtn[serverRunning="false"]:hover {
    background-color: #d0ead0;
    border-color: #3a7a3a;
}

#startStopBtn[serverRunning="true"] {
    background-color: #f0e0e0;
    color: #6a2a2a;
    border: 1px solid #8a4a4a;
}

#startStopBtn[serverRunning="true"]:hover {
    background-color: #ead0d0;
    border-color: #7a3a3a;
}

/* === Nav Bar === */
#navBar {
    background-color: palette(base);
}

#navButton {
    text-align: left;
    padding: 10px 14px;
    border: none;
    border-left: 3px solid transparent;
    border-radius: 0px;
    background: transparent;
    color: palette(window-text);
    font-size: 13px;
}

#navButton:hover:!checked {
    background: rgba(0, 0, 0, 0.04);
}

#navButton:checked {
    background: rgba(138, 112, 0, 0.10);
    border-left: 3px solid #8a7000;
    color: #8a7000;
    font-weight: bold;
}

#navHSep {
    border: none;
    background: palette(mid);
    max-height: 1px;
    min-height: 1px;
    margin: 4px 12px;
}

/* === Page Titles === */
#pageTitle {
    font-size: 18px;
    font-weight: bold;
    padding: 16px 20px 12px 20px;
    border-bottom: 1px solid palette(mid);
    color: palette(window-text);
    background: transparent;
}

/* === Placeholder labels on stub pages === */
#placeholderLabel {
    color: palette(placeholder-text);
    font-size: 14px;
}

/* === Server Setup Dialog === */
#setupDialogHeading {
    font-size: 14px;
    font-weight: bold;
}

#setupStatusLabel[status="ok"] {
    color: #2a6a2a;
    font-weight: bold;
}

#setupStatusLabel[status="error"] {
    color: #8a2a2a;
}

/* === Firewall status labels === */
QLabel[firewallStatus="ok"] {
    color: #2a6a2a;
    font-weight: bold;
}

QLabel[firewallStatus="warn"] {
    color: #8a7000;
}

QLabel[firewallStatus="blocked"] {
    color: #8a2a2a;
}

QLabel[firewallStatus="unknown"] {
    color: palette(placeholder-text);
}

/* === Server not running banner === */
#serverNotRunningBanner {
    background-color: rgba(138, 112, 0, 0.08);
    border: 1px solid rgba(138, 112, 0, 0.35);
    border-radius: 6px;
    color: #8a7000;
}

#serverNotRunningBannerIcon {
    font-size: 16px;
    color: #8a7000;
}

/* === Firewall help dialog commands === */
#helpCmds {
    font-family: "Monospace", "Courier New", monospace;
    font-size: 12px;
}

#helpNote {
    color: palette(placeholder-text);
    font-size: 12px;
}

/* === Disabled nav button === */
#navButton:disabled {
    color: palette(mid);
    background: transparent;
    border-left-color: transparent;
}

/* === Server Controls page === */
#serverOutputArea {
    background-color: #1a1d27;
    color: #c8d0c4;
    font-family: "Monospace", "Courier New", monospace;
    font-size: 12px;
    border: 1px solid palette(mid);
    border-radius: 4px;
}

#cmdPromptLabel {
    font-family: "Monospace", monospace;
    font-size: 14px;
    font-weight: bold;
    color: #8a7000;
}

#cmdInput {
    font-family: "Monospace", "Courier New", monospace;
    font-size: 13px;
}

/* === Accent button disabled state === */
QPushButton[accent="true"]:disabled {
    background-color: palette(button);
    color: palette(placeholder-text);
    border: 1px solid palette(dark);
    font-weight: normal;
}
